// enums/role.enum.ts (recommended folder path)
export enum Role {
  USER = 'user',
  CA = 'ca',
  ADMIN = 'admin',
}

export enum CaFirmType {
  INDIVIDUAL = 'individual',
  LLP = 'llp',
  PARTNERSHIP = 'partnership',
  ADVISORY = 'advisory',
}

export enum PlanType {
  BASIC = 'basic',
  ADVANCE = 'advance',
}
